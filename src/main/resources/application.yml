spring:
  application:
    name: LeaveApplication

  datasource:
    url: jdbc:postgresql://localhost:5432/leave-requests
    username: ssodev
    password: sso2022ok
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    properties:
      hibernate:
        ddl-auto: update
        format_sql: true
jwt:
  secret: my-super-secret-key-1234567890!@#$
